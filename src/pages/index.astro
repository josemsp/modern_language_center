---
const { cookies, redirect } = Astro;
const savedLang = cookies.get('user-lang')?.value;
const targetLang = savedLang && ['es', 'en'].includes(savedLang) ? savedLang : 'es';

return redirect(`/${targetLang}/`);
---