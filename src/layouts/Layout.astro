---
import "../styles/global.css";
import "../styles/carousel.css";
import BaseHead from "@/components/layout/head/index.astro";
import SVGSymbols from "@/assets/icons/SVGSymbols.astro";

interface Props {
  lang: string;
  title: string;
  description: string;
}

const { lang = "es", title, description } = Astro.props;
---

<!doctype html>
<html lang={lang}>
  <BaseHead title={title} description={description}>
    <slot name="seo-schema" />
  </BaseHead>
  <body class="w-full min-h-screen relative">
    <SVGSymbols />
    <slot name="header" />
    <slot />
    <slot name="footer" />
  </body>
</html>
