---
interface Props {
    title: string;
    description: string;
    class?: string;
}

const { title, description, class: className } = Astro.props;
---

<div class:list={["space-y-0", className]}>
    <p class="text-[#59DFB7] font-bold">{title}</p>
    <p>{description}</p>
</div>
