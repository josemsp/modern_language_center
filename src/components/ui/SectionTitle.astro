---
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<h2 class={`section-title text-[#2d2d2d] ${className}`}>
  <slot />
</h2>

<style>
  .section-title {
    font-size: clamp(40px, 5vw, 60px);
    line-height: 1;
  }
</style>
